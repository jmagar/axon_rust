description = "List indexed source URLs with chunk counts"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "sources"`
- map `{{args}}` to schema fields

Requirements:
- Parse source URLs and counts.
- Highlight key sources and anomalies.
- Keep output compact and readable.
"""
