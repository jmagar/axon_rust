description = "Scrape one or more URLs to markdown output"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "scrape"`
- map `{{args}}` to schema fields

Requirements:
- Parse per-URL scrape status and output artifact paths.
- Report skipped/failed URLs with reasons.
- Confirm embedding behavior when enabled.
- Report errors first if present.
"""
