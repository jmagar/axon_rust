description = "Discover URLs on a site without scraping content"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "map"`
- map `{{args}}` to schema fields

Requirements:
- Parse discovered URLs and coverage summary.
- Present domain/path distribution.
- Include crawl recommendations.
"""
