description = "Retrieve full document from vector database by URL"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "retrieve"`
- map `{{args}}` to schema fields

Requirements:
- Parse reconstructed content, metadata, and chunk count.
- Present completeness signals.
- Report errors first if present.
"""
