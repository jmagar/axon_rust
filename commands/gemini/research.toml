description = "Run Tavily AI research with synthesized summary"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "research"`
- map `{{args}}` to schema fields

Requirements:
- Parse synthesized findings and supporting sources.
- Highlight source quality and information gaps.
- Include follow-up recommendations when relevant.
"""
