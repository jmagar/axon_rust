description = "Run connectivity diagnostics for Axon dependencies"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "doctor"`
- map `{{args}}` to schema fields

Requirements:
- Parse each service check result.
- Report failures first with probable cause.
- Include status for Postgres, Redis, RabbitMQ, Qdrant, TEI, and LLM endpoint.
"""
