# Standalone axon_cli extraction: keep build context small.
/.git
/.github
/.cache
/.serena
/.1code
/.claude
/.codex
/.gemini
/target
/docs
/storage
/data
/specs
/.full-review
/http-cacache
/logs
/docker-build.log

# Frontend — not needed for the Rust worker image
apps
/apps
node_modules
/node_modules
**/node_modules

**/.DS_Store
**/*.log
**/*.tmp

# Secrets — never bake into the image; inject at runtime via env vars or Docker secrets.
.env
.env.*
!.env.example
