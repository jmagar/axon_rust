[[env]]
key = "QDRANT_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "compose-env-file"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Qdrant endpoint URL. URLs stay in env/CLI, not TOML."

[[env]]
key = "QDRANT_GRPC_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional Qdrant gRPC endpoint URL. URLs stay in env/CLI, not TOML."

[[env]]
key = "AXON_QDRANT_URL"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["env-template", "compose-env-file", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only Qdrant endpoint override used to point the container at a remote or explicitly local Qdrant."

[[env]]
key = "AXON_EXTERNAL_QDRANT_URL"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose-env-file", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only override consumed by docker-compose.external-qdrant.yaml to point QDRANT_URL at a remote Qdrant instead of the bundled axon-qdrant service."

[[env]]
key = "QDRANT_HTTP_PORT"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only host port for the optional local Qdrant HTTP endpoint."

[[env]]
key = "QDRANT_GRPC_PORT"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only host port for the optional local Qdrant gRPC endpoint."

[[env]]
key = "TEI_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "compose-env-file"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "TEI endpoint URL. URLs stay in env/CLI, not TOML."

[[env]]
key = "AXON_EXTERNAL_TEI_URL"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only endpoint override used by the external-provider overlay."

[[env]]
key = "AXON_CHROME_REMOTE_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "compose-env-file"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Chrome/CDP endpoint URL. URLs stay in env/CLI, not TOML."

[[env]]
key = "AXON_EXTERNAL_CHROME_REMOTE_URL"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only Chrome endpoint override used by the external-provider overlay."

[[env]]
key = "AXON_CHROME_MANAGEMENT_PORT"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only host port for the Chrome management endpoint."

[[env]]
key = "AXON_CHROME_CDP_PORT"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only host port for the Chrome CDP proxy."

[[env]]
key = "AXON_CHROME_DEVTOOLS_PORT"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only host port for raw Chrome DevTools."

[[env]]
key = "AXON_HTTP_TOKEN"
classification = "keep-env"
runtime_placement = "container-required"
surfaces = ["src", "env-template", "docs", "setup"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Runtime auth secret for MCP/action HTTP surfaces."

[[env]]
key = "AXON_AUTH_MODE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "setup"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Auth runtime mode, not non-secret tuning."

[[env]]
key = "AXON_PUBLIC_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "setup"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OAuth public origin URL. URLs stay in env."

[[env]]
key = "AXON_GOOGLE_CLIENT_ID"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OAuth client identifier."

[[env]]
key = "AXON_GOOGLE_CLIENT_SECRET"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OAuth client secret."

[[env]]
key = "AXON_AUTH_ADMIN_EMAIL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OAuth admin identity."

[[env]]
key = "AXON_ALLOWED_REDIRECT_URIS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OAuth redirect allowlist."

[[env]]
key = "AXON_ALLOWED_ORIGINS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "CORS origin allowlist for deployed HTTP surfaces."

[[env]]
key = "AXON_ENV_FILE"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "scripts", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Explicit env-file override. It can shadow ~/.axon/.env and must be detected before migration."

[[env]]
key = "AXON_CONFIG_PATH"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "setup", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Explicit config override. Trusted local operator input only."

[[env]]
key = "AXON_HOME"
classification = "trusted-operator-bootstrap"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "compose"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Axon home root. Path bootstrap, not non-secret tuning."

[[env]]
key = "AXON_DATA_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Data root path. Path bootstrap, not non-secret tuning."

[[env]]
key = "AXON_SQLITE_PATH"
classification = "trusted-operator-bootstrap"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "SQLite queue path override."

[[env]]
key = "AXON_CODE_SEARCH_ALLOWED_ROOTS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Server-side allowlist for MCP code-search cwd roots; security boundary remains process env in v1."

[[env]]
key = "AXON_SOURCE_LOCAL_ALLOWED_ROOTS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Canonical server-side allowlist for local source acquisition roots; this security boundary remains process env."

[[env]]
key = "AXON_CODE_SEARCH_FRESHNESS_TTL_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Local code-search freshness TTL in seconds; env-backed v1 tuning."

[[env]]
key = "AXON_CODE_SEARCH_REINDEX_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Foreground code-search refresh timeout in seconds; env-backed v1 tuning."

[[env]]
key = "AXON_CODE_SEARCH_MAX_FILE_BYTES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Maximum local source file size indexed by code-search; env-backed v1 tuning."

[[env]]
key = "AXON_CODE_SEARCH_CHANGED_FILE_BATCH_SIZE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Changed-file embedding batch size for code-search freshness; env-backed v1 tuning."

[[env]]
key = "AXON_HTTP_PUBLISH"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "docs", "setup"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Numeric Docker Compose host port; the mapping is fixed to loopback. Compose cannot read TOML."

[[env]]
key = "AXON_IMAGE"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose image override."

[[env]]
key = "AXON_DEV_TARGET_DIR"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Development Compose bind-mount source for the locally built Axon binary directory."

[[env]]
key = "AXON_BASE_ENV_FILE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side model-comparison runner override for the base env file copied into per-profile env files; not Axon runtime config."

[[env]]
key = "AXON_ARTIFACT_BIN_DIR"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side build-script and wrapper test override for copied binary artifact destinations; not Axon runtime config."

[[env]]
key = "AXON_AURORA_ANDROID_PATH"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test-only override used by workflow shape guards to locate the sibling Aurora Android project."

[[env]]
key = "AXON_ENABLE_HEAVY_CI"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests", "workflow"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test-only sentinel used to verify that expensive CI jobs are not controlled by a mutable runtime variable."

[[env]]
key = "GEMINI_FLASH_MODEL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side model-comparison runner override for the cli-api Gemini Flash profile model name; not Axon runtime config."

[[env]]
key = "GEMINI_3_1_FLASH_LITE_MODEL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side model-comparison runner override for the cli-api Gemini 3.1 Flash Lite profile model name; not Axon runtime config."

[[env]]
key = "GEMINI_HOME"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Host Gemini credential mount path for Compose."

[[env]]
key = "TEI_EMBEDDING_MODEL"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "setup"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "TEI container model argument."

[[env]]
key = "TEI_HTTP_PORT"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "setup"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "TEI container publish/interpolation port."

[[env]]
key = "TEI_SERVER_MAX_CLIENT_BATCH_SIZE"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "TEI server argv. Distinct from Axon TEI client batch tuning."

[[env]]
key = "NVIDIA_VISIBLE_DEVICES"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "setup"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "GPU runtime selector for Compose."

[[env]]
key = "CUDA_VISIBLE_DEVICES"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "setup"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "GPU runtime selector for Compose."

[[env]]
key = "TAVILY_API_KEY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Search/research API secret."

[[env]]
key = "AXON_SEARXNG_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Self-hosted SearXNG base URL for search/research (used instead of Tavily when set)."

[[env]]
key = "AXON_ARTIFACT_CANDIDATE_DEPOT_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional Depot endpoint for committed ArtifactCandidate delivery. URLs stay in env, not TOML."

[[env]]
key = "AXON_ARTIFACT_CANDIDATE_DEPOT_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Depot bearer credential for committed ArtifactCandidate delivery. Secrets stay in env, not TOML."

[[env]]
key = "AXON_RESEARCH_FULL_CONTENT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Toggle research full-page-content synthesis vs snippet-only."

[[env]]
key = "GITHUB_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "GitHub API token for ingest rate limits."

[[env]]
key = "GITLAB_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "GitLab API token for private project ingest and rate limits."

[[env]]
key = "GITEA_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Gitea/Forgejo API token for private repo ingest and rate limits."

[[env]]
key = "REDDIT_CLIENT_ID"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Reddit API credential."

[[env]]
key = "REDDIT_CLIENT_SECRET"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Reddit API secret."

[[env]]
key = "HF_TOKEN"
classification = "keep-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Hugging Face token for TEI model pulls."

[[env]]
key = "AXON_HEADLESS_GEMINI_CMD"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Runtime executable/bootstrap for Gemini CLI."

[[env]]
key = "AXON_HEADLESS_GEMINI_HOME"
classification = "trusted-operator-bootstrap"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Gemini credential home override."

[[env]]
key = "AXON_HEADLESS_GEMINI_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Runtime Gemini model selection."

[[env]]
key = "AXON_SYNTHESIS_HEADLESS_GEMINI_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Gemini model override for RAG synthesis. Non-secret runtime selector."

[[env]]
key = "AXON_CHAT_HEADLESS_GEMINI_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Gemini model override for direct chat. Falls back to the synthesis model when unset."

[[env]]
key = "AXON_CODEX_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Legacy alias for the Codex app-server synthesis model. Non-secret runtime selector."

[[env]]
key = "AXON_SYNTHESIS_CODEX_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Codex app-server model override for RAG synthesis. Non-secret runtime selector."

[[env]]
key = "AXON_CODEX_COMPLETION_CONCURRENCY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Max concurrent Codex app-server child completions. Defaults lower because each completion spawns a child process."

[[env]]
key = "AXON_CODEX_POOL_IDLE_TTL_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Idle TTL (seconds) before the Codex app-server pool discards an unused child on next checkout. 0 disables TTL eviction."

[[env]]
key = "AXON_CODEX_LOAD_USER_CONFIG"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Opt-in: run codex app-server against the user's real CODEX_HOME (MCP servers, skills, hooks) instead of the isolated stripped home. Non-secret runtime toggle, default false."

[[env]]
key = "AXON_LLM_COMPLETION_CONCURRENCY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret LLM completion tuning. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_LLM_COMPLETION_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret LLM completion tuning. Kept in env until a typed config.toml field is wired."

[[env]]
key = "TEI_MAX_CLIENT_BATCH_SIZE"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.batch-size"
compatibility = "warn-env-override"
reason = "TEI client request batch size. Distinguish from TEI server argv in Compose."

[[env]]
key = "TEI_MAX_RETRIES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.max-retries"
compatibility = "warn-env-override"
reason = "TEI client retry tuning."

[[env]]
key = "TEI_REQUEST_TIMEOUT_MS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.request-timeout-ms"
compatibility = "warn-env-override"
reason = "TEI client timeout tuning."

[[env]]
key = "AXON_TEI_RETRY_BACKOFF_MS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.retry-backoff-ms"
compatibility = "warn-env-override"
reason = "Base backoff (ms) before exponential growth + jitter between retried TEI `/embed` requests on 429/5xx."

[[env]]
key = "AXON_TEI_COOLDOWN_AFTER_FAILURES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.cooldown-after-failures"
compatibility = "warn-env-override"
reason = "Consecutive TEI failures before the provider self-reports a cooldown."

[[env]]
key = "AXON_TEI_COOLDOWN_SECS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.cooldown-secs"
compatibility = "warn-env-override"
reason = "Cooldown window length (seconds) once the TEI failure threshold trips."

[[env]]
key = "AXON_TEI_INTERACTIVE_RESERVED_REQUESTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.interactive-reserved-requests"
compatibility = "warn-env-override"
reason = "Embedding reservation slots reserved for interactive (ask/query) embeddings out of max-concurrent-requests."

[[env]]
key = "AXON_TEI_BACKGROUND_MAX_CONCURRENT_REQUESTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.background-max-concurrent-requests"
compatibility = "warn-env-override"
reason = "Soft per-tier concurrency ceiling for background-priority embed requests."

[[env]]
key = "AXON_TEI_MAINTENANCE_MAX_CONCURRENT_REQUESTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.maintenance-max-concurrent-requests"
compatibility = "warn-env-override"
reason = "Soft per-tier concurrency ceiling for maintenance-priority embed requests (migrate/reindex-class jobs)."

[[env]]
key = "AXON_TEI_QUERY_INSTRUCTION_ENABLED"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.query-instruction-enabled"
compatibility = "warn-env-override"
reason = "Apply the query/document instruction prefix when the embedding provider capability supports it."

[[env]]
key = "AXON_SCRAPE_BATCH_TIMEOUT_SECS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "scrape.batch-timeout-secs"
compatibility = "warn-env-override"
reason = "End-to-end timeout for one service-level scrape batch."

[[env]]
key = "AXON_ALLOW_UNBOUNDED_BROAD_CRAWL"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "scrape.allow-unbounded-broad-crawl"
compatibility = "warn-env-override"
reason = "Explicit operator override for uncapped broad-domain crawl guardrails."

[[env]]
key = "AXON_CRAWL_MEMORY_ABORT_PERCENT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "scrape.crawl-memory-abort-percent"
compatibility = "warn-env-override"
reason = "RSS percent of host/cgroup memory that aborts long-running crawls."

[[env]]
key = "AXON_INGEST_LANES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.ingest-lanes"
compatibility = "warn-env-override"
reason = "Worker lane tuning."

[[env]]
key = "AXON_EMBED_LANES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.embed-lanes"
compatibility = "warn-env-override"
reason = "Worker lane tuning."

[[env]]
key = "AXON_EMBED_DOC_TIMEOUT_SECS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.embed-doc-timeout-secs"
compatibility = "warn-env-override"
reason = "Per-document embed timeout tuning."

[[env]]
key = "AXON_EMBED_CACHE_ENABLED"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.cache-enabled"
compatibility = "warn-env-override"
reason = "Opt-in content-addressed embedding cache toggle."

[[env]]
key = "AXON_EMBED_CACHE_MAX_ENTRIES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.cache-max-entries"
compatibility = "warn-env-override"
reason = "Maximum retained content-addressed embedding cache entries."

[[env]]
key = "AXON_UNIFIED_WORKER_CONCURRENCY"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.unified-worker-concurrency"
compatibility = "warn-env-override"
reason = "Bounds how many unified job-store jobs the in-process worker runs concurrently (clamped 1-64)."

[[env]]
key = "AXON_QUEUE_SUMMARY_SECS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.queue-summary-secs"
compatibility = "warn-env-override"
reason = "Queue summary interval tuning."

[[env]]
key = "AXON_QDRANT_POINT_BUFFER"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "pipeline.qdrant-point-buffer"
compatibility = "warn-env-override"
reason = "Legacy compatibility fallback for providers.vector.upsert-batch-points."

[[env]]
key = "AXON_MAX_PENDING_CRAWL_JOBS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.max-pending-crawl-jobs"
compatibility = "warn-env-override"
reason = "Queue cap tuning."

[[env]]
key = "AXON_MAX_PENDING_EMBED_JOBS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.max-pending-embed-jobs"
compatibility = "warn-env-override"
reason = "Queue cap tuning."

[[env]]
key = "AXON_MAX_PENDING_EXTRACT_JOBS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.max-pending-extract-jobs"
compatibility = "warn-env-override"
reason = "Queue cap tuning."

[[env]]
key = "AXON_MAX_PENDING_INGEST_JOBS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "workers.max-pending-ingest-jobs"
compatibility = "warn-env-override"
reason = "Queue cap tuning."

[[env]]
key = "AXON_ASK_AUTHORITATIVE_BOOST"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "providers.vector.upsert-batch-points"
compatibility = "canonical"
reason = "Canonical Qdrant upsert request batch size; wired into typed config."

[[env]]
key = "AXON_ASK_AUTHORITATIVE_DOMAINS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_BACKFILL_CHUNKS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_CANDIDATE_LIMIT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "ask.candidate-limit"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_ASK_CHUNK_LIMIT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "ask.chunk-limit"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_ASK_DOC_CHUNK_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_DOC_FETCH_CONCURRENCY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_FULL_DOCS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_QUALITY_MIN_CANONICAL_SOURCES"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side ask-quality smoke-test assertion threshold; not Axon runtime config."

[[env]]
key = "AXON_ASK_QUALITY_MIN_FULL_DOCS"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side ask-quality smoke-test assertion threshold; not Axon runtime config."

[[env]]
key = "AXON_ASK_QUALITY_QUERY"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side ask-quality smoke-test query override; not Axon runtime config."

[[env]]
key = "AXON_ASK_HYBRID_CANDIDATES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "scripts", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "search.ask-hybrid-candidates"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_ASK_MAX_CONTEXT_CHARS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_MIN_CITATIONS_NONTRIVIAL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_ASK_MIN_RELEVANCE_SCORE"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "ask.min-relevance-score"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_BIN"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_CHROME_DIAGNOSTICS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_CHROME_DIAGNOSTICS_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_CHROME_DIAGNOSTICS_EVENTS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_CHROME_DIAGNOSTICS_SCREENSHOT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_CHROME_PROXY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_CHROME_USER_AGENT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "scripts", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_USER_AGENT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "tests"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "General-purpose HTTP User-Agent override for host/container HTTP clients, Chrome fallback, structured API clients, and tests. Kept in env so runtime wrappers share the same identity."

[[env]]
key = "AXON_COLLECTION"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "tests", "scripts", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_MEMORY_COLLECTION"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Dedicated memory Qdrant collection override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_DOMAINS_DETAILED"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_DOMAINS_DETAILED_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_DOMAINS_FACET_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_EMBED_POOL_MAX_INPUTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.pool-max-inputs"
compatibility = "warn-env-override"
reason = "Typed provider embedding pool capacity is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EMBED_PREP_CONCURRENCY"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.prep-concurrency"
compatibility = "warn-env-override"
reason = "Typed provider embedding preparation concurrency is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_PREP_MAX_IN_FLIGHT_BYTES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.prep-max-in-flight-bytes"
compatibility = "warn-env-override"
reason = "Typed preparation memory admission is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EMBED_SCHEDULER_FLUSH_MS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.scheduler-flush-ms"
compatibility = "warn-env-override"
reason = "Typed embedding scheduler flush delay is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EMBED_SCHEDULER_ENABLED"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.scheduler-enabled"
compatibility = "warn-env-override"
reason = "Typed bounded-scheduler selection is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_TEI_CLIENT_MAX_BATCH_TOKENS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.max-batch-tokens"
compatibility = "warn-env-override"
reason = "Typed TEI client batch-token capacity is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_VECTOR_UPSERT_EMBED_OVERLAP"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.vector-upsert-overlap-enabled"
compatibility = "warn-env-override"
reason = "Typed vector-upsert overlap selection is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EMBED_PREPARED_BYTE_BUDGET"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.prepared-byte-budget"
compatibility = "warn-env-override"
reason = "Typed prepared-generation memory admission budget is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EMBED_MAX_CHUNKS_PER_DOC"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.max-chunks-per-doc"
compatibility = "warn-env-override"
reason = "Typed provider embedding chunk-volume guard is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EMBED_MAX_SOURCE_CHUNKS_PER_DOC"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.max-source-chunks-per-doc"
compatibility = "warn-env-override"
reason = "Typed provider embedding source-chunk guard is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_RESIDUAL_GAP_FLOOR"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Min stripped-char count for an inter-declaration residual gap to survive as a prose chunk in the declaration-driven code chunker (src/vector/ops/input/code/assembly/residual.rs). Non-secret deterministic chunking knob; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_EMBED_DEDUPE_EXACT_CHUNKS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.dedupe-exact-chunks"
compatibility = "warn-env-override"
reason = "Typed provider embedding dedupe policy is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_EXCLUDE_PATH_PREFIX"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_HNSW_EF_SEARCH"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "search.hnsw-ef"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_LOG_COLOR"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose color toggle for container log output. Compose cannot read TOML."

[[env]]
key = "REDDIT_SECRET"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests"]
secret = true
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "test-fixture"
reason = "Synthetic redaction fixture value used in tests; runtime credential key is REDDIT_CLIENT_SECRET."

[[env]]
key = "AXON_HNSW_EF_SEARCH_LEGACY"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "search.hnsw-ef-legacy"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_HYBRID_CANDIDATES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "search.hybrid-candidates"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_HYBRID_SEARCH"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "search.hybrid-enabled"
compatibility = "warn-env-override"
reason = "Non-secret tuning knob; migrate to config.toml."

[[env]]
key = "AXON_INLINE_BYTES_THRESHOLD"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_INSTALL_DRY_RUN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_INSTALL_PREFIX"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_INSTALL_SKIP_SETUP"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_INSTALL_TMPDIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_INSTALL_URL"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_IN_CONTAINER"
classification = "compose-env"
runtime_placement = "container-required"
surfaces = ["src", "compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Container bootstrap marker supplied by Compose."

[[env]]
key = "AXON_JOB_STALE_CONFIRM_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_JOB_STALE_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_JOB_WAIT_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_JOBS_AUTO_WORKER"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob (also jobs.auto-worker in config.toml); env wins. Gates auto-spawn of a background worker after a detached CLI enqueue."

[[env]]
key = "AXON_JOBS_WORKER_IDLE_EXIT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob (also jobs.worker-idle-exit-secs in config.toml); env wins. Idle seconds before an auto-started worker exits."

[[env]]
key = "AXON_ALLOW_INCOMPATIBLE_STORE_STARTUP"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Operator/dev escape hatch that bypasses the cutover store-compatibility startup gate (crates/axon-core/src/health/doctor/cutover.rs); env-only, never a config.toml field."

[[env]]
key = "AXON_TASK_RESULT_WAIT_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret MCP task result wait tuning knob; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_API_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Authenticated REST live-E2E target URL override; harness-only and not a production runtime key."

[[env]]
key = "AXON_API_EVIDENCE_DIR"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Optional evidence output directory for the REST live-E2E harness."

[[env]]
key = "AXON_MCP_BINARY"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Optional Axon binary override used by MCP live-wire harnesses."

[[env]]
key = "AXON_MCP_TASK_OUTDIR"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Optional evidence output directory for the MCP task live-wire harness."

[[env]]
key = "AXON_LIVE_TEST_OUTDIR"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_LIVE_CHROME_REMOTE_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Explicit Chrome DevTools endpoint override for the isolated live CLI harness."

[[env]]
key = "AXON_LIVE_CHROME_PORT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Optional fixed port for harness-owned Chrome; the default lets Chrome allocate a collision-safe port."

[[env]]
key = "AXON_LIVE_CHROME_SESSION_TOKEN"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Internal process-ownership nonce used to make live harness Chrome cleanup fail closed."

[[env]]
key = "AXON_LOCAL_MODE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_SERVER_URL"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "removed"
reason = "Removed in 5.0.0 (CLI/MCP client-server forwarding deleted); no longer read at runtime. Retained here only for migration tracking because legacy compose/getting-started docs still mention it."

[[env]]
key = "AXON_SUPPRESS_STALE_BINARY_WARNING"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "When set, suppresses the stale-binary startup warning emitted by src/core/binary_status.rs. Read directly via env; non-secret runtime toggle."

[[env]]
key = "AXON_LOG_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_LOG_FILE"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "scripts", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_LOG_PATH"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "scripts", "docs", "env-template"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Canonical log file path; overrides AXON_LOG_DIR/AXON_LOG_FILE composition. Bootstrap override read once at startup."

[[env]]
key = "AXON_LOG_FULL_QUERIES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_LOG_LEVEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_LOG_MAX_BYTES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_MAX_TREE_SITTER_FILE_BYTES"
classification = "keep-env"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Advanced process-local guard for symbol extraction CPU/memory cost. Kept in env until a typed config.toml field is justified."

[[env]]
key = "AXON_LOG_MAX_FILES"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_MCP"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_MCP_ARTIFACT_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_MCP_EMBED_ALLOWED_ROOTS"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_MCP_EMBED_MAX_LOCAL_BYTES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_MCP_EMBED_MAX_LOCAL_DEPTH"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_MCP_EMBED_MAX_LOCAL_ENTRIES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_HTTP_HOST"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "scripts", "docs", "compose"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_HTTP_PORT"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "scripts", "docs", "compose"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_MCP_TRANSPORT"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "scripts", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_NEO4J_URL"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_NO_COLOR"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "scripts", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_NO_WIPE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_OUTPUT_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_QDRANT_UPSERT_BATCH_SIZE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_QDRANT_QUANTIZATION_ENABLED"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example", "scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "qdrant.quantization-enabled"
compatibility = "warn-env-override"
reason = "Typed Qdrant quantization tuning belongs in config.toml; the environment remains a compatibility override."

[[env]]
key = "AXON_QDRANT_ASYNC_WRITES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example", "scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "qdrant.async-writes"
compatibility = "warn-env-override"
reason = "Typed Qdrant write-wait policy belongs in config.toml; the environment remains a compatibility override."

[[env]]
key = "AXON_QDRANT_TRANSPORT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example", "scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "qdrant.transport"
compatibility = "warn-env-override"
reason = "Typed Qdrant transport selection belongs in config.toml; the environment remains a compatibility override."

[[env]]
key = "AXON_QDRANT_UPSERT_PARALLELISM"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "providers.vector.write-concurrency"
compatibility = "warn-env-override"
reason = "Typed Qdrant write concurrency belongs in config.toml; the environment remains a compatibility override."

[[env]]
key = "AXON_QDRANT_BULK_LOAD"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Fresh-collection bulk-load profile; intentionally env-only until operator policy and crash recovery semantics are finalized."

[[env]]
key = "AXON_QDRANT_BULK_INDEXING_THRESHOLD_KB"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Temporary Qdrant indexing threshold used only when AXON_QDRANT_BULK_LOAD is enabled."

[[env]]
key = "AXON_QDRANT_INDEXING_THRESHOLD_KB"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Restored Qdrant indexing threshold after AXON_QDRANT_BULK_LOAD embed pipelines finish."

[[env]]
key = "AXON_QDRANT_HNSW_M"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Fresh-collection HNSW graph connectivity tuning. Must be quality-tested before changing defaults."

[[env]]
key = "AXON_QDRANT_HNSW_EF_CONSTRUCT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Fresh-collection HNSW build-effort tuning. Must be quality-tested before changing defaults."

[[env]]
key = "AXON_QDRANT_PAYLOAD_INDEX_PROFILE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Fresh collection payload-index set selector; core profile is useful for docs-only benchmark/import workflows."

[[env]]
key = "AXON_QDRANT_PAYLOAD_INDEX_PARALLELISM"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "advanced"
reason = "Payload-index creation fanout tuning for collection initialization."

[[env]]
key = "AXON_REPO_ROOT"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_SERVER_INSECURE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_SESSION_INGEST_MAX_BYTES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_SETUP_SKIP_SMOKE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_SOURCES_FACET_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_REFRESH_FACET_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; caps the seed_url facet breadth scanned by source refresh. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_SOURCES_DOMAIN_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret export cap for explicit domain source listing; migrate to config.toml when a typed field is wired."

[[env]]
key = "AXON_SUGGEST_BASE_URL_LIMIT"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_SUGGEST_EXISTING_URL_LIMIT"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_SUGGEST_INDEX_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_TARGET_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_TEI_MAX_CONCURRENT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.max-concurrent-requests"
compatibility = "warn-env-override"
reason = "Typed provider embedding concurrency is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_TEST_AMQP_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_ASK_TEXT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BASE_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BATCH_URL_1"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BATCH_URL_2"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BATCH_URL_3"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BOOL_OPT_ABSENT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BOOL_OPT_FALSE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_BOOL_OPT_TRUE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_DOCKER_FAIL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_EXTRACT_PROMPT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_FALLTHROUGH_PROBE_VAR"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_GITHUB_REPO"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_HOME_ENV_VAR_LOADED"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_HOME_UNSET_PROBE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_LOG"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_PG_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_PRECEDENCE_VAR"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_QDRANT_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src", "scripts", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_QUERY_TEXT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_REDDIT_TARGET"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_REDIS_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["tests"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_SEARCH_TEXT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_SYMLINK_REJECT_PROBE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_TAVILY_KEY_ABSENT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_TAVILY_KEY_PRESENT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_TEST_YOUTUBE_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "AXON_WATCHDOG_SWEEP_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_WORKER_STARVATION_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret worker-liveness tuning knob (starvation detector threshold); also settable via config.toml (workers.worker-starvation-secs)."

[[env]]
key = "AXON_CRAWL_JOB_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret per-crawl-job wall-clock timeout knob; also settable via config.toml (workers.crawl-job-timeout-secs)."

[[env]]
key = "AXON_MAX_JOB_ATTEMPTS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret worker-liveness tuning knob (max reclaim attempts before a stale job is dead-lettered); also settable via config.toml (workers.max-job-attempts)."

[[env]]
key = "AXON_WEB_ALLOWED_ORIGINS"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "AXON_WEB_API_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["scripts"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Secret or credential required at runtime; stays in env."

[[env]]
key = "CUDA_CACHE_DISABLE"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "GEMINI_API_KEY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Secret or credential required at runtime; stays in env."

[[env]]
key = "GEMINI_AUTH_FILES"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "GEMINI_CLI_TRUST_WORKSPACE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "GITHUB_INGEST_TOTAL_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "GITHUB_MAX_ISSUES"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "GITHUB_MAX_PRS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "GITHUB_REPO"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "GITHUB_SUBTASK_COUNT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "HF_HUB_CACHE"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "HF_HUB_ENABLE_HF_TRANSFER"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "NVIDIA_REQUIRE_CUDA"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "QDRANT_COLLECTION"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "URL/path/bootstrap override; env is allowed when supplied by the operator or runtime wrapper."

[[env]]
key = "QDRANT_PRODUCTION_YAML"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "REDDIT_BATCH_FLUSH_SIZE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "REDDIT_CLIENT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "REDDIT_TARGET"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Test/dev harness variable, not a production env template key."

[[env]]
key = "REDDIT_USER_AGENT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "TEI_CONCURRENCY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "TEI_MAX_BACKOFF_MS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Non-secret tuning knob; migrate to config.toml. Kept in env until a typed config.toml field is wired."

[[env]]
key = "TEI_MAX_BATCH_REQUESTS"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "TEI_MAX_BATCH_TOKENS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Secret or credential required at runtime; stays in env."

[[env]]
key = "TEI_MAX_CONCURRENT_REQUESTS"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "TEI_POOLING"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Docker Compose interpolation or container-only server argument."

[[env]]
key = "TEI_TOKENIZATION_WORKERS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Secret or credential required at runtime; stays in env."

[[env]]
key = "GOOGLE_API_KEY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Google/Gemini API key secret used by headless LLM auth."

[[env]]
key = "GOOGLE_APPLICATION_CREDENTIALS"
classification = "trusted-operator-bootstrap"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Path to Google ADC credentials used by headless LLM auth."

[[env]]
key = "GOOGLE_CLOUD_LOCATION"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Google Vertex AI runtime location setting."

[[env]]
key = "GOOGLE_CLOUD_PROJECT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Google Vertex AI runtime project setting."

[[env]]
key = "GOOGLE_GENAI_USE_VERTEXAI"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Google GenAI Vertex AI runtime mode switch."

[[env]]
key = "GOOGLE_OAUTH_ALLOWED_EMAILS"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "OAuth protection test harness variable."

[[env]]
key = "GOOGLE_OAUTH_CLIENT_ID"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "OAuth protection test harness variable."

[[env]]
key = "GOOGLE_OAUTH_CLIENT_SECRET"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = true
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "OAuth protection test harness secret variable."

[[env]]
key = "GOOGLE_OAUTH_REDIRECT_HOST"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "OAuth protection test harness variable."

[[env]]
key = "AXON_ENABLE_VERTICALS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "verticals.enabled"
compatibility = "warn-env-override"
reason = "Webclaw vertical dispatch feature gate. Operator tuning — move to config.toml."

[[env]]
key = "AXON_AUTO_DISPATCH_SKIP"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "verticals.auto-dispatch-skip"
compatibility = "warn-env-override"
reason = "Comma-separated list of vertical names to skip during auto-dispatch. Operator tuning."

[[env]]
key = "AXON_STRUCTURED_DATA_MAX_BYTES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "payload.structured-data-max-bytes"
compatibility = "warn-env-override"
reason = "Max bytes for structured data extraction payload. Operator tuning."

[[env]]
key = "AXON_LADDER_STRATEGY1_THRESHOLD"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "scrape.ladder-strategy1-threshold"
compatibility = "warn-env-override"
reason = "Multi-strategy DOM retry ladder threshold for strategy 1. Operator tuning."

[[env]]
key = "AXON_LADDER_STRATEGY2_THRESHOLD"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "scrape.ladder-strategy2-threshold"
compatibility = "warn-env-override"
reason = "Multi-strategy DOM retry ladder threshold for strategy 2. Operator tuning."

[[env]]
key = "AXON_LADDER_BODY_MULTIPLIER"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "scrape.ladder-body-multiplier"
compatibility = "warn-env-override"
reason = "Body size multiplier for DOM ladder strategy selection. Operator tuning."

[[env]]
key = "AXON_CHALLENGE_WARMUP"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "antibot.cookie-warmup"
compatibility = "warn-env-override"
reason = "Enable antibot cookie warmup on challenge-page detection. Operator tuning."

[[env]]
key = "AXON_ANTIBOT_MAX_BODY_SCAN_BYTES"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-toml", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "antibot.max-body-scan-bytes"
compatibility = "warn-env-override"
reason = "Max bytes to scan for antibot challenge signals in response body. Operator tuning."

[[env]]
key = "AXON_VERTICAL_CACHE_TTL_GITHUB"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Per-vertical cache TTL override (dynamic prefix AXON_VERTICAL_CACHE_TTL_<NAME>=secs). GITHUB is the documented example; other vertical names follow the same pattern."

[[env]]
key = "AXON_ENV_FILE_RESOLVED"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Script-internal variable set by scripts/axon to track the resolved env file path. Not read by the Rust binary."

[[env]]
key = "AXON_VERTICAL"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "CLI shortcut env var read by `axon scrape` to force a specific vertical extractor by name (e.g. AXON_VERTICAL=amazon). One-shot operator override; not threaded through Config."

[[env]]
key = "OPENAI_API_KEY"
classification = "codex-child-auth"
runtime_placement = "child-only"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Ignored by Axon config; optional auth fallback forwarded only to the isolated `codex app-server` child."

[[env]]
key = "OPENAI_BASE_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts", "tests"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "external"
reason = "Not part of the Axon runtime. Referenced only by standalone helper scripts and tests that assert removed OpenAI runtime env is ignored."

[[env]]
key = "OPENAI_MODEL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts", "tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "external"
reason = "Not part of the Axon runtime. Referenced only by standalone helper scripts and tests that assert removed OpenAI runtime env is ignored."

[[env]]
key = "AXON_ENDPOINT_BUNDLE_CONCURRENCY"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "stable"
reason = "Process-wide semaphore cap for concurrent endpoint bundle fetches (default 8). Operator-tunable."

[[env]]
key = "AXON_ENDPOINT_CHROME_CONCURRENCY"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "stable"
reason = "Process-wide semaphore cap for concurrent Chrome capture sessions (default 1). Operator-tunable."

[[env]]
key = "AXON_ENDPOINT_VERIFY_CONCURRENCY"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "stable"
reason = "Process-wide semaphore cap for concurrent endpoint verification sessions (default 16). Operator-tunable."

[[env]]
key = "AXON_ENDPOINT_PROBE_CONCURRENCY"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "stable"
reason = "Process-wide semaphore cap for concurrent JSON-RPC/MCP/ACP endpoint probe sessions (default 4, min 1). Operator-tunable."

[[env]]
key = "AXON_LLM_BACKEND"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Selects the LLM completion backend: 'gemini-headless' (default), 'openai-compat', or 'codex-app-server'. Non-secret runtime selector."

[[env]]
key = "AXON_SYNTHESIS_HIGH_CONTEXT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional tri-state override (true/false/unset) for whether the synthesis model is treated as high-context; when unset the model profile auto-detects. Env wins over the TOML [llm] synthesis-high-context form."

[[env]]
key = "AXON_UPDATE_MINISIGN_PUBKEY"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Mandatory release-authentication key embedded into published binaries and required by the installer; installation and self-update fail closed when absent. Host-side only."

[[env]]
key = "AXON_OPENAI_BASE_URL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OpenAI-compatible API root URL, required when AXON_LLM_BACKEND=openai-compat. URLs stay in env/CLI, not TOML."

[[env]]
key = "AXON_OPENAI_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Model name for the OpenAI-compatible backend, required when AXON_LLM_BACKEND=openai-compat. Non-secret runtime selector."

[[env]]
key = "AXON_SYNTHESIS_OPENAI_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OpenAI-compatible model override for RAG synthesis. Non-secret runtime selector."

[[env]]
key = "AXON_CHAT_OPENAI_MODEL"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "OpenAI-compatible model override for direct chat. Falls back to the synthesis model when unset."

[[env]]
key = "AXON_OPENAI_API_KEY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional API key for the OpenAI-compatible backend. Runtime secret, env-only."

[[env]]
key = "AXON_PROVIDER"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Selects the active saved LLM provider profile before falling back to config.toml."

[[env]]
key = "AXON_UPDATE_INSTALL_PATH"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side axon update install destination override; useful for controlled local installs and smoke tests."

[[env]]
key = "AXON_UPDATE_FILE_RELEASE_DIR"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side update smoke-test override pointing at a directory with local release assets."

[[env]]
key = "AXON_CODEX_CMD"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Operator-supplied codex app-server executable path for the experimental Codex backend."

[[env]]
key = "AXON_CODEX_HOME"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "docs", "compose"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Operator-supplied Codex auth home copied into an isolated app-server home."

[[env]]
key = "AXON_CODEX_CONTROL_ENABLED"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Explicit opt-in for the trusted Codex app-server management runtime used by Palette."

[[env]]
key = "AXON_CODEX_CONTROL_HOME"
classification = "trusted-operator-bootstrap"
runtime_placement = "host-only"
surfaces = ["src", "env-template", "docs"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Absolute service-owned non-symlink home for the trusted Codex control runtime."

[[env]]
key = "AXON_CODEX_CONTROL_REQUEST_TIMEOUT_SECS"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Bounded request timeout for Codex control app-server calls."

[[env]]
key = "AXON_CODEX_CONTROL_READ_CONCURRENCY"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Maximum concurrent read-only Codex control calls."

[[env]]
key = "AXON_CODEX_CONTROL_ACCOUNT_WRITES"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Fine-grained opt-in for Codex account mutations."

[[env]]
key = "AXON_CODEX_CONTROL_CONFIG_WRITES"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Fine-grained opt-in for Codex configuration mutations."

[[env]]
key = "AXON_CODEX_CONTROL_MCP_WRITES"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Fine-grained opt-in for Codex MCP server mutations."

[[env]]
key = "AXON_CODEX_CONTROL_PLUGIN_WRITES"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Fine-grained opt-in for Codex plugin mutations."

[[env]]
key = "AXON_CODEX_CONTROL_SKILL_WRITES"
classification = "keep-env"
runtime_placement = "host-only"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Fine-grained opt-in for Codex skill mutations."

[[env]]
key = "AXON_WATCH_TICK_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Watch scheduler sweep interval in seconds (default 15, min 1); non-secret tuning knob. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_WATCH_LEASE_SECS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Watch scheduler lease TTL in seconds (default 300, min 1); non-secret tuning knob. Kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_FS_NAMESPACE"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "compose-env-file", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Embed-job claim affinity namespace; set per-deployment (compose) so workers only claim jobs for their namespace. Deployment placement knob, stays in env."

[[env]]
key = "AXON_ETAG_MAX_MISS_RUNS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Crawl ETag conditional re-crawl: max consecutive missed runs before a cached page is re-fetched. Non-secret tuning knob; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_QDRANT_HNSW_ON_DISK"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Qdrant collection HNSW on-disk toggle (trades latency for RAM safety on large collections). Non-secret tuning knob; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_QDRANT_QUANTIZATION_ALWAYS_RAM"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "docs"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Qdrant int8 quantization always-in-RAM toggle (memory vs latency for the quantized vectors). Non-secret tuning knob; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_TEI_MAX_IN_FLIGHT_INPUTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.max-in-flight-inputs"
compatibility = "warn-env-override"
reason = "Typed provider embedding in-flight input capacity is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_MARKDOWN_CHUNK_MIN_CHARS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Minimum target markdown chunk size. Non-secret chunking tuning; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_MARKDOWN_CHUNK_MAX_CHARS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Maximum prose/markdown chunk size. Non-secret chunking tuning; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_CHUNK_OVERLAP_CHARS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template", "docs", "config-example"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Adjacent prose/markdown chunk overlap. Non-secret chunking tuning; kept in env until a typed config.toml field is wired."

[[env]]
key = "AXON_OPENAI_EMBEDDING_MODEL"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.openai-model"
compatibility = "warn-env-override"
reason = "OpenAI-compatible embedding model is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_OPENAI_EMBED_MAX_CLIENT_BATCH_SIZE"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.openai-max-client-batch-size"
compatibility = "warn-env-override"
reason = "OpenAI-compatible embedding request batch size is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_OPENAI_EMBED_MAX_CONCURRENT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.openai-max-concurrent"
compatibility = "warn-env-override"
reason = "OpenAI-compatible embedding request concurrency is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_OPENAI_EMBED_MAX_IN_FLIGHT_INPUTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.openai-max-in-flight-inputs"
compatibility = "warn-env-override"
reason = "OpenAI-compatible embedding in-flight input capacity is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "AXON_OPENAI_EMBED_POOL_MAX_INPUTS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "config-example"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "providers.embedding.openai-pool-max-inputs"
compatibility = "warn-env-override"
reason = "OpenAI-compatible embedding input pool size is wired in config.toml; the env key remains accepted as a compatibility override."

[[env]]
key = "OPENAI_EMBED_CONCURRENCY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "legacy-alias"
reason = "Legacy alias for OpenAI-compatible embedding request concurrency."

[[env]]
key = "OPENAI_EMBED_IN_FLIGHT_INPUTS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "legacy-alias"
reason = "Legacy alias for OpenAI-compatible embedding in-flight input cap."

[[env]]
key = "OPENAI_EMBED_IN_FLIGHT_INPUT_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "legacy-alias"
reason = "Legacy alias for OpenAI-compatible embedding in-flight input cap."

[[env]]
key = "TEI_IN_FLIGHT_INPUTS"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "legacy-alias"
reason = "Legacy alias for AXON_TEI_MAX_IN_FLIGHT_INPUTS."

[[env]]
key = "TEI_IN_FLIGHT_INPUT_LIMIT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "legacy-alias"
reason = "Legacy alias for AXON_TEI_MAX_IN_FLIGHT_INPUTS."

[[env]]
key = "AXON_AGENT_OS_EXPECTED_FINAL_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS harness variable; not Axon runtime config."

[[env]]
key = "AXON_AGENT_OS_HOST_IP"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS harness variable; not Axon runtime config."

[[env]]
key = "AXON_AGENT_OS_SQLITE_NAME"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS harness variable; not Axon runtime config."

[[env]]
key = "AXON_AGENT_OS_STAGING_PORT"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS harness variable; not Axon runtime config."

[[env]]
key = "AXON_AGENT_OS_TARGET_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS harness variable; not Axon runtime config."

[[env]]
key = "AXON_SCRIPT_EXPECTED_FINAL_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS helper-script variable; not Axon runtime config."

[[env]]
key = "AXON_SCRIPT_HOST_BASE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS helper-script variable; not Axon runtime config."

[[env]]
key = "AXON_SCRIPT_PHASE"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS helper-script variable; not Axon runtime config."

[[env]]
key = "AXON_SCRIPT_RELEASE_TAG"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS helper-script variable; not Axon runtime config."

[[env]]
key = "AXON_SCRIPT_SQLITE_NAME"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS helper-script variable; not Axon runtime config."

[[env]]
key = "AXON_SCRIPT_TARGET_URL"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Chrome-extension Agent OS helper-script variable; not Axon runtime config."

[[env]]
key = "HF_HOME"
classification = "external/test-only"
runtime_placement = "not-runtime"
surfaces = ["scripts"]
secret = false
url_or_path = true
container_allowed = false
toml_destination = ""
compatibility = "advanced"
reason = "Host-side embedding experiment cache override used by scripts; not Axon runtime config."

[[env]]
key = "AXON_SOURCE_JOB_CONCURRENCY_LIMIT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "docs", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "pipeline.max-active-source-jobs"
compatibility = "warn-env-override"
reason = "Caps concurrently running source jobs across every family plus map. Renamed from AXON_CRAWL_JOB_CONCURRENCY_LIMIT; canonical TOML spelling is pipeline.max-active-source-jobs."

[[env]]
key = "AXON_CRAWL_JOB_CONCURRENCY_LIMIT"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["docs"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = "pipeline.max-active-source-jobs"
compatibility = "warn-env-override"
reason = "Removed clean-break name. The knob gated every source job kind, not only crawls. Replaced by AXON_SOURCE_JOB_CONCURRENCY_LIMIT / pipeline.max-active-source-jobs."

[[env]]
key = "AXON_JOBS_RETENTION_EVENT_DAYS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.event-retention-days"
compatibility = "warn-env-override"
reason = "Retention window for job events. Deletes operator data on a schedule."

[[env]]
key = "AXON_JOBS_RETENTION_FAILED_EVENT_DAYS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.failed-event-retention-days"
compatibility = "warn-env-override"
reason = "Retention window for failed job events. Deletes operator data on a schedule."

[[env]]
key = "AXON_JOBS_RETENTION_TERMINAL_DAYS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.terminal-retention-days"
compatibility = "warn-env-override"
reason = "Retention window for terminal job rows. Deletes operator data on a schedule."

[[env]]
key = "AXON_JOBS_RETENTION_PROVIDER_HEALTH_DAYS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.provider-health-retention-days"
compatibility = "warn-env-override"
reason = "Retention window for provider health samples. Deletes operator data on a schedule."

[[env]]
key = "AXON_JOBS_RETENTION_ARTIFACT_DAYS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.artifact-retention-days"
compatibility = "warn-env-override"
reason = "Retention window for job artifacts. Deletes operator data on a schedule."

[[env]]
key = "AXON_JOBS_RETENTION_SWEEP_SECS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.retention-sweep-secs"
compatibility = "warn-env-override"
reason = "Interval between retention sweeps that apply the jobs.*-retention-days windows."

[[env]]
key = "AXON_JOBS_INTERACTIVE_STARVATION_SLO_SECS"
classification = "move-toml"
runtime_placement = "not-runtime"
surfaces = ["src", "env-template"]
secret = false
url_or_path = false
container_allowed = true
toml_destination = "jobs.interactive-starvation-slo-secs"
compatibility = "warn-env-override"
reason = "Starvation SLO for the interactive job lane. Currently inert: no transport can set JobPriority::Interactive."

[[env]]
key = "AXON_COMPOSE_PROJECT_NAME"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "scripts", "tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only project identity used to isolate Axon stacks and test harnesses."

[[env]]
key = "AXON_CONTAINER_NAME"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "scripts", "tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only Axon service container-name override."

[[env]]
key = "AXON_QDRANT_CONTAINER_NAME"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "scripts", "tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only Qdrant service container-name override."

[[env]]
key = "AXON_TEI_CONTAINER_NAME"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "scripts", "tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only TEI service container-name override."

[[env]]
key = "AXON_CHROME_CONTAINER_NAME"
classification = "compose-env"
runtime_placement = "compose-interpolation"
surfaces = ["compose", "scripts", "tests"]
secret = false
url_or_path = false
container_allowed = false
toml_destination = ""
compatibility = "canonical"
reason = "Compose-only Chrome service container-name override."
[[env]]
key = "QDRANT_API_KEY"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "scripts"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Qdrant bearer credential used by runtime and trusted live E2E providers."

[[env]]
key = "AXON_CHROME_BEARER_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "scripts"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Bearer credential for an authenticated remote Chrome management endpoint."

[[env]]
key = "AXON_TEI_BEARER_TOKEN"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "scripts"]
secret = true
url_or_path = false
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Bearer credential for an authenticated remote TEI endpoint."

[[env]]
key = "AXON_GOOGLE_AUTHORIZE_ENDPOINT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional Google-compatible OAuth authorization endpoint override."

[[env]]
key = "AXON_GOOGLE_TOKEN_ENDPOINT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional Google-compatible OAuth token endpoint override."

[[env]]
key = "AXON_GOOGLE_JWKS_ENDPOINT"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional Google-compatible OIDC JWKS endpoint override."

[[env]]
key = "AXON_GOOGLE_ISSUER"
classification = "keep-env"
runtime_placement = "both"
surfaces = ["src", "env-template"]
secret = false
url_or_path = true
container_allowed = true
toml_destination = ""
compatibility = "canonical"
reason = "Optional Google-compatible OIDC issuer override."
