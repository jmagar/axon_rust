description = "Check Axon async queue and worker status"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "status"`
- map `{{args}}` to schema fields

Requirements:
- Parse queue and worker health.
- Present blocked or failing lanes first.
- Include pending/running/failed counts when available.
"""
