description = "Show Qdrant collection and indexing statistics"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "stats"`
- map `{{args}}` to schema fields

Requirements:
- Parse collection-level metrics.
- Highlight size/capacity signals and notable changes.
- Keep output compact and readable.
"""
