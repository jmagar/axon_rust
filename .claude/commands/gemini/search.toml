description = "Search the web and enqueue crawl jobs from results"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "search"`
- map `{{args}}` to schema fields

Requirements:
- Parse ranked results, snippets, and URLs.
- Confirm crawl enqueue behavior from returned results.
- Present top hits and next actions.
- Report errors first if present.
"""
