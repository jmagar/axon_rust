description = "Ask questions grounded in indexed docs (AI-powered Q&A)"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "ask"`
- map `{{args}}` to schema fields

Requirements:
- Present the synthesized answer first.
- Then list supporting sources/citations.
- Include retrieval/context metrics when available.
- Report errors first if present.
"""
