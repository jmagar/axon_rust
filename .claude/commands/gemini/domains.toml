description = "List indexed domains and document stats"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "domains"`
- map `{{args}}` to schema fields

Requirements:
- Parse domain-level counts and totals.
- Present highest-volume domains first.
- Keep output compact and readable.
"""
