description = "Semantic search over embedded content in Qdrant"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "query"`
- map `{{args}}` to schema fields

Requirements:
- Parse ranked results, similarity scores, snippets, and source URLs.
- Present top results in relevance order.
- Summarize dominant themes and refinements.
- Report errors first if present.
"""
