description = "Capture webpage screenshot through Axon action"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "screenshot"`
- map `{{args}}` to schema fields

Requirements:
- Parse screenshot artifact path and metadata.
- Report navigation/render failures clearly.
"""
