description = "Show Axon help and action usage"
prompt = """
Use the Axon MCP server tool (`axon`) with:
- `action: "help"`
- map `{{args}}` to schema fields

Requirements:
- If no args are provided, use top-level help.
- Highlight available commands and usage examples.
- Recommend the next most relevant command for the current goal.
"""
