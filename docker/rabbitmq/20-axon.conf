# rabbitmq:4.0-management enables the management plugin by default.
# Disable the deprecated management metrics collection mode introduced in 4.x.
# The management UI remains available with Prometheus-based stats instead.
management.disable_stats = true
